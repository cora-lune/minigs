{% block body %}

    <div class="span3">
        {% include 'administration/layoutAdmin.html.twig' %}
    </div>

<div class="container">
    <div class="row">
        <div class="span9">
        <form action="{{ path('productsEdition') }}" method="post">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit">Modifier !</button>
            {{ form_end(form) }}
        </form>
    </div>
    </div>
</div>

{% endblock %}


        {% block footer %}
            {% include 'Administration/footerAdmin.html.twig' %}
        {% endblock %}